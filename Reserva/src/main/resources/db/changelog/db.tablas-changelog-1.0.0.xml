<?xml version="1.0" encoding="UTF-8"?>
<!--otra forma de escribir versiones de liquibase-->
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">
    
    <changeSet id="tag-1" author="julito">
        <tagDatabase tag="1.0.0-1.0"/>
    </changeSet>
    
     <changeSet id="creacion-tabla-users" author="julito" tag="1.0.0">
         <comment>creacion de la tabla users</comment>  
         <sql>
            create table users(
                id serial primary key not null,
                email varchar(100) not null,
                first_name varchar(50) not null,
                last_name varchar(50) not null,
                password varchar(100) not null,
                birthDate date not null,
                natioality varchar (100)
            );
         </sql>
     <!--para ejecutar el comando de roolback: mvn liquibase:tag -Dliquibase.tag=1.0.0-->
     <!--
        \liquibase -(-van dos guiones bajo pero me saca el cometario)
                    changeLogFile=db.tablas-changelog-1.0.0.xml
                    -(-van dos guiones bajo pero me saca el cometario)
                    outputFile=db.outputChangelog-1.0.0.xml(optional) 
                    rollbackSQL 1.0.0 (tag)
         -->
    <rollback>
            DROP TABLE IF EXISTS poo2020_reserva_hotel.users;
    </rollback>
    </changeSet>
    
    
    
</databaseChangeLog>
